{
  "routes": [
    {
      "method": "GET",
      "path": "/health-messages",
      "handler": "health-message.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/health-messages/count",
      "handler": "health-message.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/health-messages/:id",
      "handler": "health-message.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/health-messages",
      "handler": "health-message.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/health-messages/:id",
      "handler": "health-message.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/health-messages/:id",
      "handler": "health-message.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
